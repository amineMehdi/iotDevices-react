import React from "react";

function Navbar(props){
    return (
        <nav>
            <div className= "logo">
                Image
            </div>

            <div>
                <button>Add Device</button>
            </div>
        </nav>
    );
}

export default Navbar;